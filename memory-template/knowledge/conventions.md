# 设计 / 前端约定

> 层：**项目知识**（长期有效，与谁在做无关）｜总览见仓库根 `AGENTS.md`。
> 改样式、改令牌、改页面前读这里。

<!--
填写指引：
- 只写「当前规则」，**不写变更史**（谁反馈的、第几轮、试过什么 → 放变更史文档）。
- 适合进来的：令牌入口与命名、禁止项、唯一实现（「X 只有一个实现」）、易记错的命名。
- 本文件是「约束型」知识 —— 删了会让 AI 做错事，所以宁可写短也别删。
-->

## 令牌 / 变量

- <!-- 例：禁止硬编码色值，一律走 `path/to/tokens`。唯一集中例外：<哪里> -->
- <!-- 例：<某档令牌> 用于 <场景>，不要用它做 <另一场景>（对比度不足）。 -->

## 唯一实现（同一个东西只有一份）

- <!-- 例：卡片只有一个配方 `@mixin card-glass`；SFC 里 `@extend` 无效（scoped 是独立编译单元）→ 用 `@include`。 -->
- <!-- 例：空态只有 `components/EmptyState.vue`；结果态另一个组件，两者同页互斥。 -->

## 易记错的命名 / 取值

- <!-- 例：排序白名单键是 a/b/c（调用时用 c，不是 d）。 -->
