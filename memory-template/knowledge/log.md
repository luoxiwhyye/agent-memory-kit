# 历史流水（详细版）

> 层：**项目知识 · 历史类**（长期有效，但只在追溯时读）｜总览见仓库根 `AGENTS.md`。
> ⚠️ **仅供追溯「当时怎么做的 / 为什么这么做」，写代码前请勿通读。**
> 环境坑见 `pitfalls.md`，验证手段见 `verify.md`，进度/待办见 `../status/ops.md`。

<!--
填写指引：
- 「详细」指**保留可追溯的事实链**，不是原文照搬。保留：根因 / 关键取舍（为什么选 A 不选 B）/
  踩到的坑（错误做法 → 正确做法）/ 涉及文件与提交。
- 可以压掉：重复罗列验证命令、同一结论的多种表述、纯试错过程（除非试错本身就是坑）。
- 按「主题分组」而不是按「时间顺序」—— 时间顺序在 git log 里。
- 一旦某结论升级成「当前规则」，就把它**搬到** conventions/backend 等子文件，
  这里只留一行指针，避免两处维护。
-->

## 〇、架构与基线

<!-- 例：v1 → v2 的关键转折、当前所处的形态、README 版本记录的读法。 -->

## 一、<主题>

<!-- 每节格式建议：问题 → 根因 → 做法 → 关键坑。 -->
