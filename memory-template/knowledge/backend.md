# 服务端 / 数据契约

> 层：**项目知识**（长期有效，与谁在做无关）｜总览见仓库根 `AGENTS.md`。
> 改接口、改数据访问、改鉴权、改缓存前读这里。
> 没有服务端 / 数据层的项目：删掉本文件，并删掉 `AGENTS.md` 索引表里的对应行。

<!--
填写指引：
- 记「契约」而不是「实现细节」：谁调用谁、字段含义、边界与错误形状、哪些操作不可逆。
  实现细节看代码就够，写进来只会过期。
- 适合进来的：接口契约、数据模型与约束、鉴权模型、缓存策略、外部依赖、
  迁移与版本兼容规则、幂等性要求。
- 只写「当前规则」，不写变更史；契约变了就改这里，不留旧值。
- 本节标题是**形式示例**：按你的项目换主题（如 CLI 参数契约、消息格式、配置 schema），用不到的删掉。
-->

## 接口契约

- <!-- 例：所有列表接口统一返回 `{ items, total }`，不接受别的形状。 -->
- <!-- 例：破坏性变更必须换版本号，不能原地改字段含义。 -->
- <!-- 例：错误统一为 <某形状>；调用方只认 `code`，不要依赖 `message` 文案。 -->

## 数据模型与约束

- <!-- 例：<某表/集合> 的唯一键、软删除、时区约定；跨表一致性靠 <某机制> 保证。 -->
- <!-- 例：时间统一存 UTC，只在展示层转本地时区。 -->

## 鉴权 / 权限

- <!-- 例：<某角色> 能做什么、不能做什么；权限判断只在 <某一层> 做，别处不重复判断。 -->

## 缓存 / 一致性

- <!-- 例：<某数据> 缓存多久；<某写操作> 必须同时失效 <哪些键>，否则读到旧值。 -->

## 外部依赖 / 集成

- <!-- 例：调 <第三方> 的超时、重试、降级策略；沙箱与生产的行为差异。 -->

## 迁移 / 兼容

- <!-- 例：加字段要可空且带默认值；删字段先停用，跨两个发布周期才能真删。 -->
