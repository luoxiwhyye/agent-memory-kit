# 部署 / 运行环境

> 层：**项目知识**（长期有效，与谁在做无关）｜总览见仓库根 `AGENTS.md`。
> 部署、发布、回滚、恢复前读这里。
> 还没上线的项目 / 纯本地工具：删掉本文件，并删掉 `AGENTS.md` 索引表里的对应行。
> 「此刻部署到了什么状态」属于**个人状态**，写 `../status/ops.md`，不要写在这里。

<!--
填写指引：
- 只记「怎么安全地做」和「环境之间差在哪」，不记流水账式发布记录（那些在 git tag / 发布日志里）。
- **禁止写入凭据**（口令 / token / 私钥 / cookie）：只写「去哪里取、由谁管」。
- 适合进来的：环境拓扑、发布与回滚流程、配置来源、备份恢复、
  必须人工确认的步骤、已知不可逆操作。
- 本节标题是**形式示例**：按你的项目换主题（如分发渠道、桌面打包、固件烧录），用不到的删掉。
-->

## 环境拓扑

- <!-- 例：本地 / 测试 / 生产分别跑在哪；域名与端口的对应关系。 -->
- <!-- 例：<某服务> 与 <某服务> 必须同机，不能拆开部署。 -->

## 发布流程

- <!-- 例：<构建命令> → <发布命令>；发布前必须先过 <某检查>。 -->
- <!-- 例：顺序有依赖：先 <A> 后 <B>，反过来会 <后果>。 -->

## 配置与密钥

- <!-- 例：配置来源是 <路径 / 环境变量>；密钥由 <谁> 管，本机不落盘。 -->
- <!-- 例：改完配置必须 <重启 / 重载> 才生效。 -->

## 回滚

- <!-- 例：回滚命令 <命令>；<某操作> 不可逆，回滚前必须先 <备份>。 -->

## 备份 / 恢复

- <!-- 例：备份频率与位置；恢复演练怎么做；恢复后要核对 <哪些量>。 -->

## 运维禁忌

- <!-- 例：不要在 <时段> 做 <操作>。 -->
- <!-- 例：不要手工改 <生成物 / 线上文件>，下次发布会被覆盖。 -->
