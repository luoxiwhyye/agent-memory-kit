# 进度 / 待办（会过期）

> 层：**个人状态**（会过期，与「此刻做到哪了」有关）｜总览见仓库根 `AGENTS.md`。
> ⚠️ **闭环后当天删掉对应段落**；长期规则请回写 `../knowledge/` 对应子文件。
> 给人看的进度明细放在项目自己的文档里，这里只留**指针 + 待办**。

<!--
填写指引：
- 只写「此刻状态」：进行中 / 阻塞 / 下一步 / 待定项。**不要追加历史**。
- 一屏以内。长到几十 KB 说明设计失败（该拆到项目文档里，这里只留链接）。
- 「下一步」尽量引用已有条目的编号，而不是抄写正文。
- **禁止写入凭据**（密码 / token / API key / 私钥 / cookie）。需要提醒时只写「去哪里查」。
- ⚠️ 别把已完成的明细往这里堆 —— 那属于 `knowledge/log.md` 或项目文档。
-->

## 当前任务

## 已完成（压缩到一行以内，明细指向别处）

## 待办 / 待定项

## 当前状态（环境 / 部署 / 数据）

## 更新时间
