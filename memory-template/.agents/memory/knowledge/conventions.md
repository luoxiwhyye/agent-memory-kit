# 项目约定

> 层：**项目知识**（长期有效，与谁在做无关）｜总览见仓库根 `AGENTS.md`。
> 动手改代码、配置、资源前读这里。

<!--
填写指引：
- 只写「当前规则」，**不写变更史**（谁反馈的、第几轮、试过什么 → 放变更史文档）。
- 适合进来的：命名与取值规范、禁止项、唯一实现（「X 只有一个实现」）、易记错的 API 与字段。
- 本文件是「约束型」知识 —— 删了会让 AI 做错事，所以宁可写短也别删。
- 下面几节是**形式示例**：按你的项目换主题，用不到的直接删，别留空标题。
-->

## 命名 / 取值规范

- <!-- 例：禁止硬编码 <某类值>（颜色 / 尺寸 / 超时 / 路径），一律走 <唯一入口>。唯一集中例外：<哪里> -->
- <!-- 例：<某常量> 只有一处定义，别处只能引用，不要就地写第二个字面量。 -->

## 唯一实现（同一个东西只有一份）

- <!-- 例：<某能力>（分页 / 格式化 / 鉴权 / 空态 UI …）只有一个实现 `<路径>`；复制第二份会导致 <后果>。 -->
- <!-- 例：<甲> 与 <乙> 互斥，同一处只能出现一个（如两种结果态）→ 由 <某处> 统一决定。 -->

## 禁止项（做了会出错）

- <!-- 例：不要直接改 <生成物 / 构建产物>，改 <源> 后重新生成。 -->
- <!-- 例：不要绕过 <某一层> 直接访问 <底层>。 -->
- <!-- 例：依赖管理只有一个入口 <命令>，不要混用其它工具的锁文件。 -->

## 易记错的命名 / 参数

- <!-- 例：<某 API> 的参数顺序 / 单位 / 是否含边界，容易和 <另一个> 混。 -->
- <!-- 例：<甲字段> 与 <乙字段> 名字相近但含义不同：<甲> 是 <…>，<乙> 是 <…>。 -->
