# 环境 / 工具坑

> 层：**项目知识**（长期有效，与谁在做无关）｜总览见仓库根 `AGENTS.md`。
> 按「现象 → 根因 / 对策」一行一条。**踩到怪现象时查这里，不要凭直觉改代码。**

<!--
填写指引：
- 一条只讲一件事。写成「现象 → 根因 → 对策」，不要写排查过程。
- 只收「下次还会踩」的；一次性的偶发问题不要进来。
- 判据：**遇到这个现象的人会不会浪费时间？** 会 → 值得写。
- 分节按「工具域」而不是按项目（语言 / 构建 / 终端 / 数据 / 网络 / 运行 / 前端…），
  这样跨项目复制时结构不用改。**用不到的小节直接删掉，不留空标题。**
-->

## 0. 通用（跨域）

- <!-- 例：工具 X 在 Y 情况下会返回空，而不是报错 → 别据此判定「文件不存在」。 -->

## A. 语言 / 框架

## B. 构建 / 依赖 / 文件锁

## C. 终端 / 脚本 / 工具链

## D. 数据 / 存储 / 缓存

## E. 网络 / 接口 / 集成

## F. 运行 / 部署 / 容器

## G. 前端 / 浏览器 / UI
